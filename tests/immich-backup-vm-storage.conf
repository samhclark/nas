# ABOUTME: Isolates the exact-image VM fixture from image-defined stores.
[storage]
driver = "overlay"
runroot = "/run/nas-immich-backup-vm-smoke/containers-run"
graphroot = "/var/lib/nas-immich-backup-vm-smoke/containers-storage"

[storage.options.overlay]
mountopt = "nodev"
