[project]
name = "nas-dev"
version = "0.0.0"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "ty==0.0.69",
]

[tool.uv]
package = false
required-version = "==0.12.9"

[tool.ty.environment]
python-version = "3.14"

[tool.ty.src]
include = ["generate-quadlets.py", "quadletgen", "scripts/plan-image-cleanup.py"]

[tool.ty.rules]
all = "error"
